package HashTable_Channing;

class Entry {
    Object key; // key
    Object value; // value

    Entry(Object key, Object value) { // entry 객체 생성자로 key, value를 받으면 저장
        this.key = key;
        this.value = value;
    }
}
